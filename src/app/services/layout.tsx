import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services | Hariz Crane Trucks | Crane Hire, Transport & Lifting Sydney",
  description: "Crane hire, machinery transport, container moves, material delivery, heavy loads and urgent lifts across Sydney. Get a free quote in 15 minutes.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
